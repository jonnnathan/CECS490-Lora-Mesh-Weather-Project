# Python dependencies for LoRa Mesh Serial Bridge
# Install with: pip install -r requirements.txt

pyserial>=3.5       # Serial port communication
websockets>=11.0    # WebSocket server for real-time updates
aiohttp>=3.9        # HTTP server for dashboard
requests>=2.31      # ThingSpeak API uploads (optional)
